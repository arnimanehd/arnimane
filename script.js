const TWITCH_CHANNEL="Arnimane";const followBtn=document.getElementById("followBtn");const followMenu=document.getElementById("followMenu");followBtn.addEventListener("click",e=>{e.stopPropagation();followMenu.classList.toggle("open")});document.addEventListener("click",e=>{if(!followMenu.contains(e.target)&&e.target!==followBtn)followMenu.classList.remove("open")});const parent=window.location.hostname||"localhost";document.getElementById("twitchPlayer").src=`https://player.twitch.tv/?channel=${TWITCH_CHANNEL}&parent=${parent}&muted=false`;function esc(t=""){return String(t).replace(/[&<>"']/g,s=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[s]))}function formatDate(d){return new Date(d).toLocaleDateString(undefined,{month:"short",day:"numeric",year:"numeric"})}function durationLabel(seconds){const m=Math.floor(seconds/60),s=Math.floor(seconds%60);return `${m}:${String(s).padStart(2,"0")}`}function videoCard(v,isShort=false){return `<article class="${isShort?"short-card":"video-card"}"><a href="${v.url}" target="_blank" rel="noreferrer"><div class="thumb"><img src="${v.thumbnail}" alt=""><span class="duration">${isShort?"":durationLabel(v.durationSeconds)}</span></div><h3>${esc(v.title)}</h3><small>${formatDate(v.publishedAt)}</small></a></article>`}async function loadUploads(){try{const r=await fetch("/api/youtube"),d=await r.json();if(!r.ok)throw new Error(d.error||"Could not load uploads");const videos=d.videos||[],shorts=d.shorts||[];document.getElementById("videosGrid").innerHTML=videos.slice(0,3).map(v=>videoCard(v,false)).join("")||"<p class='loading'>No videos found.</p>";document.getElementById("shortsGrid").innerHTML=shorts.slice(0,4).map(v=>videoCard(v,true)).join("")||"<p class='loading'>No Shorts found.</p>"}catch(e){document.getElementById("videosGrid").innerHTML="<p class='loading'>Add your YouTube API environment variables in Vercel to load videos.</p>";document.getElementById("shortsGrid").innerHTML="<p class='loading'>Shorts will appear here after setup.</p>"}}async function loadTwitchStatus(){try{const r=await fetch("/api/twitch-status"),d=await r.json();if(d?.title)document.getElementById("streamTitle").textContent=d.title;if(d?.live){document.getElementById("streamStatus").textContent=`Playing ${d.game||"games"}${d.viewers?` with ${d.viewers.toLocaleString()} viewers`:""}`}else document.getElementById("streamStatus").textContent="Offline right now — check out the latest uploads."}catch{document.getElementById("streamStatus").textContent="Live status ready to connect."}}loadUploads();loadTwitchStatus();